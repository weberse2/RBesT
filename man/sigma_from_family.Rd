% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sigma_from_family.R
\name{sigma_from_family}
\alias{sigma_from_family}
\title{Compute sigma as a function of eta for a GLM family}
\usage{
sigma_from_family(eta, family, offset = 0, phi = 1)
}
\arguments{
\item{eta}{Numeric vector of values on the link scale (parameter of interest).}

\item{family}{An R family object (e.g., binomial(), MASS::negative.binomial(0.5)).}

\item{offset}{Numeric scalar. Shift applied before evaluating the variance
function. For NB/Poisson with log link, this is log(exposure). Default 0.}

\item{phi}{Numeric scalar. Dispersion parameter. For gaussian family this is
sigma^2 (user-supplied). For binomial/NB/Poisson this is 1 (the default).}
}
\value{
Numeric vector of sigma values (same length as eta).
}
\description{
For a GLM family with variance function V(mu), link function g, and
dispersion parameter phi, the sampling standard deviation of the MLE
on the link scale is:
sigma(eta) = sqrt(phi * V(mu)) / |dmu/deta|
where mu = linkinv(eta + offset).
}
\keyword{internal}
